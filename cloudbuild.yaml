steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'taibt2docker/reactappdemo:$COMMIT_SHA', '.']
    dir: './'

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'taibt2docker/reactappdemo:$COMMIT_SHA']